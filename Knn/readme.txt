datingTestSet.txt是测试数据
datingTrainingSet.txt是训练数据

我们的目的是用训练数据训练模型，然后用测试数据玩模型
主要输出预测数据的准确率

思路整理：
首先建立一个解析器，把文件读入pathon中，输入文件名，输出文件中的矩阵
然后建立一个分类器，输入一行测试数据路径和训练数据路径，输出预测结果

编程时总结的KNN的缺点：
1.labels的离散化不是很有效，可以改进
2.k=值
3.计算量大
4.需要各特征独立

学到的python好用的方法：
1.字典的get()函数可以给出默认值
2.列表的append和extend方法
3.array的**方法
4.map()方法一次性对列表的所有值使用函数，但是之后要用list转换回列表
5.argsort()方法返回排序的索引
6.字典的sorted函数
7.if __name__=='__main__':
8.感觉使用列表非常不方便，下次试着用array和dataframe编程










